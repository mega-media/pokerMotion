export const PADDING = 60;
export const ELE_WIDTH = 228;
export const ELE_HEIGHT = 348;
export const AREA_SIZE = 20;
export const APP_WIDTH = ELE_HEIGHT + PADDING * 2;
export const APP_HEIGHT = ELE_HEIGHT + PADDING * 2;
export const ORIGIN_POINTS =  [
  [APP_WIDTH / 2, APP_HEIGHT / 2],
  [APP_WIDTH / 2 + ELE_WIDTH, APP_HEIGHT / 2],
  [APP_WIDTH / 2 + ELE_WIDTH, APP_HEIGHT / 2 + ELE_HEIGHT],
  [APP_WIDTH / 2 , APP_HEIGHT / 2 + ELE_HEIGHT]
]
